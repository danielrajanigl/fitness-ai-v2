# Supabase Configuration
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Ollama Configuration
# For local: OLLAMA_HOST=http://localhost:11434
# For remote Mac: OLLAMA_HOST=http://192.168.x.x:11434
# For remote server: OLLAMA_HOST=http://your-server-ip:11434
OLLAMA_HOST=http://localhost:11434

# App Configuration
NODE_ENV=development

# ⚠️ Cloudflare Deployment Note:
# If deploying to Cloudflare Pages/Workers, Ollama connections may not work
# because Cloudflare Workers have restrictions on external connections.
# Solutions:
# 1. Deploy to Vercel, Railway, or another platform that supports external connections
# 2. Use Cloudflare Workers with external connections (if available)
# 3. Set up Ollama on a publicly accessible server with proper security

# Cloudflare Tunnel Example (for exposing local Ollama)
# Run: cloudflared tunnel --url http://localhost:11434
# Then use the provided URL:
# OLLAMA_HOST=https://your-tunnel-url.trycloudflare.com
